const user = {
    name: "Marc",
    age: 27
};

const getName = () => user.name;

const getAge = () => user.age;

const updateUser = () => (user.age = 28);

// hard to debug
// hard to test

// What is the functional way?
